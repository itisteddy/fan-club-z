-- Staging parity: ensure prediction and option columns expected by API exist.

ALTER TABLE IF EXISTS public.predictions
  ADD COLUMN IF NOT EXISTS category TEXT NULL,
  ADD COLUMN IF NOT EXISTS category_id UUID NULL,
  ADD COLUMN IF NOT EXISTS type TEXT NULL,
  ADD COLUMN IF NOT EXISTS stake_min NUMERIC NULL,
  ADD COLUMN IF NOT EXISTS stake_max NUMERIC NULL,
  ADD COLUMN IF NOT EXISTS settlement_method TEXT NULL,
  ADD COLUMN IF NOT EXISTS is_private BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS tags TEXT[] NULL,
  ADD COLUMN IF NOT EXISTS likes_count INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS comments_count INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS image_url TEXT NULL;

ALTER TABLE IF EXISTS public.prediction_options
  ADD COLUMN IF NOT EXISTS total_staked NUMERIC(18,8) NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS current_odds NUMERIC(12,6) NOT NULL DEFAULT 2.0;

