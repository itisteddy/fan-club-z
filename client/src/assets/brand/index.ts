/**
 * Canonical brand assets for Fan Club Z
 * All logo references should import from this file for consistency
 */

// Logo mark (square icon) - use for app icons, favicons, etc.
export { default as LogoMarkPng } from './logo-mark.png';

// Logo wordmark (wide version) - use for headers, landing pages
export { default as LogoWordmarkPng } from './logo-wordmark.png';

// SVG version (if available) - use for scalable UI elements
// Note: if no real SVG is provided, fall back to the mark PNG.
export { default as LogoSvg } from './logo-mark.png';
