{
  "name": "fan-club-z",
  "private": true,
  "version": "2.0.75",
  "description": "Root workspace package for Fan Club Z",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "preinstall": "npm --prefix server install",
    "build": "npm --prefix server run build",
    "start": "npm --prefix server run start",
    "dev": "npm --prefix server run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

